jenkins:
  credentials:
    system:
      domainCredentials:
        - credentials:
          - usernamePassword:
            id: "artifactory"
            username: "admin"
            password: "password"
            scope: GLOBAL
  securityRealm:
    local:
      users:
        - id: "panda"
          password: "panda"
          name: "panda"
  authorizationStrategy:
    globalMatrix:
      permissions:
        - Overall/Administer:panda
  numExecutors: 0

