jenkins:
  securityRealm:
    local:
      users:
        - id: "panda"
        password: "panda"
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "USER:Overall/Administer:panda"
  numExecutors: 0

